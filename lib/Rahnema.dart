import 'package:flutter/material.dart';

class Rahnema extends StatelessWidget {
  const Rahnema({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
    );
  }
}
